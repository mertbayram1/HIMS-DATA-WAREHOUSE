# HIMS Frontend (scaffold)

This is a minimal React + Vite + TypeScript + Tailwind scaffold for the HIMS project.

Quick start (requires Node 18+):

```bash
cd frontend
npm install
npm run dev
```

The app will run on `http://localhost:5173` by default.
